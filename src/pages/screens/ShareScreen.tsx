// CommentScreen.tsx

import React from "react";
import { View, Text } from "react-native";

const ShareScreen: React.FC = () => {
  return (
    <View>
      <Text>This is the share Screen</Text>
    </View>
  );
};

export default ShareScreen;

// Add an empty export statement at the end
export {};
